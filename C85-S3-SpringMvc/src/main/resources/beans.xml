<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mybatis="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="
	http://www.springframework.org/schema/beans
    https://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    https://www.springframework.org/schema/context/spring-context.xsd
    http://www.springframework.org/schema/mvc
    https://www.springframework.org/schema/mvc/spring-mvc.xsd
     http://mybatis.org/schema/mybatis-spring 
     http://mybatis.org/schema/mybatis-spring.xsd">

	<!-- 组件扫描 -->
	<context:component-scan
		base-package="com.zj.springmvc"></context:component-scan>

	<!-- SpringMVC注解驱动标签 -->
	<mvc:annotation-driven></mvc:annotation-driven>

	<!-- 静态页面标签 -->
	<mvc:default-servlet-handler />

	<!-- location:被映射的路径，工程外的路径 classpath:/META-INF/public-web-resources/ 映射类路径中的文件夹 
		"file:/F:/images/"映射本地文件夹 mapping:映射后的路径 工程内部路径 -->
	<mvc:resources location="file:/E:/images/"
		mapping="/photo/**"></mvc:resources>


	<!-- 配置文件上传解析器 -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">

		<!-- 最大上传文件大小 100M左右 1801 4032=18M -->
		<property name="maxUploadSize" value="100000000" />

	</bean>

	<!-- 拦截器定义 -->
	<!-- <mvc:interceptors> <mvc:interceptor> -->
	<!-- 这里定义的地址不是servlet的URL定义 -->
	<!-- <mvc:mapping path="/*.do"/>拦截 <mvc:mapping path="/reg.html"/>拦截 <mvc:exclude-mapping 
		path="regs.do"/>不拦截 <bean class="com.zj.springmvc.web.LoginInterceptor"></bean> 
		</mvc:interceptor> </mvc:interceptors> -->
		
		
     <!-- 配置数据源 -->
    <context:property-placeholder location="jdbc.properties"/>
	<bean id="dataSource"
		class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
		<property name="driverClassName"
			value="${jdbc.driverClassName}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
	</bean>
	<!-- mybatis工厂会话bean -->
	<bean id="sqlSessionFactory"
		class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
	</bean>
	
	<!-- 发现映射器  映射器扫描 -->
	<mybatis:scan base-package="com.yc.damai.Dao"/>

</beans>
